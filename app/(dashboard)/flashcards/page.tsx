import React from 'react'

function Flashcards() {
  return (
    <div>
      in Flashcards
      we might need to move the AuthProvider from app/layout to dashboard/layout
    </div>
  )
}

export default Flashcards
